# 描述生成 SVG 图形

这是一个纯前端网页程序：输入文字描述（如“戴围巾的橘猫”），页面会自动生成对应的 SVG 图形，并支持预览、复制和下载。

## 使用方式

1. 打开 `index.html`。
2. 在输入框写描述词（中文/英文都可）。
3. 点击“生成 SVG”。
4. 复制 SVG 代码或下载 `.svg` 文件。

## 说明

- 当前为本地规则生成，不依赖后端。
- 内置了猫、狗、鱼、鸟、花、树、房子、机器人等对象识别。
- 未识别的描述会自动生成抽象风格 SVG。
